var $   = require( '../node_modules/jquery' )
  , log = require( './index' );

$(function () {
    // window.log = log;
    console.log( '----- log: ', log );
    // log.setLevel( log.LEVELS.TRACE );
    // log.debug( 'This is trace log record' );

    // log.setLevel( log.LEVELS.DEBUG );
    // log.debug( 'This is debug log record' );
});
